<div class="text-center"><h1>inspectAhouse</h1>
   <p>You can't be in <%= @inspections.count %> places at once, find an inspecta! </p>


   <div id="map"
      style="width: 100%; height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
   </div>

</div>